# liuxianmao

Turborepo monorepo initialized with a Next.js website app and shared package configuration.

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm lint
pnpm test
pnpm type-check
pnpm assets:build
```

`pnpm assets:build` 会将 `docs/extracted-svg-icons` 中已登记的原始素材标准化到网站运行时资源目录。
