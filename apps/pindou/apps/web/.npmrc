# 国内网络访问 npm 官方源较慢，使用镜像避免大体积依赖下载超时。
registry=https://registry.npmmirror.com/

# 为偶发网络抖动保留足够的重试和单次请求时间。
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
fetch-timeout=120000
